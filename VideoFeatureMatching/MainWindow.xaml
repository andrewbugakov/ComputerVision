<Window x:Class="VideoFeatureMatching.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel
            Orientation="Horizontal" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <Button x:Name="captureButton" Content="Capture" Click="captureButtonClick" Margin="10"/>
            <Button Content="Flip Horizontal" Click="FlipHorizontalButtonClick" Margin="10"/>
            <Button Content="Flip Vertical" Click="FlipVerticalButtonClick" Margin="10"/>
        </StackPanel>
        <Image
            x:Name="captureImageBox" Grid.Row="1" Grid.Column="0"/>
        <Image
            x:Name="grayscaleImageBox" Grid.Row="2" Grid.Column="0"/>
        <Image
            x:Name="smoothedGrayscaleImageBox" Grid.Row="1" Grid.Column="1"/>
        <Image
            x:Name="cannyImageBox" Grid.Row="2" Grid.Column="1"/>
    </Grid>
</Window>
